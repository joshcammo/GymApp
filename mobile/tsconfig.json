{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    // TypeScript 6 no longer pulls in every @types package automatically.
    "types": ["node", "react"],
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
